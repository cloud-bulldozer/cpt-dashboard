imagePullSecrets: []
nameOverride: ""
fullnameOverride: "dashboard"
replicaCount: 1

ingress:
  enabled: true
  hostName: dashboard
  baseDomain: apps.sailplane.perf.lab.eng.rdu2.redhat.com

elasticsearch:
  # Use existing secret
  existingSecretName: "dashboard-secret"

frontend:
  image:
    repository: default-route-openshift-image-registry.apps-crc.testing/cpt-dashboard/frontend
    tag: latest
  resources: {}

backend:
  image:
    repository: default-route-openshift-image-registry.apps-crc.testing/cpt-dashboard/backend
    tag: latest
  resources: {}
  securityContext: {}

podAnnotations:
  alpha.image.policy.openshift.io/resolve-names: '*'

podSecurityContext: {}
